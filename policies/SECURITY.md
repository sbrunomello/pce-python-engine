# Security Policy

- Nunca commitar segredos ou credenciais.
- Todos os eventos externos devem ser validados por JSON Schema.
- Persistência local em SQLite deve evitar SQL dinâmico.
- CI executa verificação básica de segredos.
